BINANCE_API_KEY=your_binance_api_key_here
BINANCE_SECRET_KEY=your_binance_secret_key_here

TG_BOT_TOKEN=your_telegram_bot_token_here
TG_CHAT_ID=your_telegram_chat_id_here
# 额外推送群组 (逗号分隔多个群的 Chat ID, 可选)
TG_GROUP_IDS=-1001234567890,-1009876543210

# AI 分析 (通过 OpenAI 兼容接口, 可选 — 不配置则 AI 功能自动禁用)
AI_API_KEY=your_api_key_here
AI_API_BASE=https://your-litellm-proxy.com/v1
AI_MODEL=your-model-name

# CRITICAL 告警 SMTP 降级 (可选 — 不配置则降级为 log.error)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=alert@example.com
SMTP_PASS=your_smtp_password_here
ALERT_EMAIL=you@example.com

# 外部心跳 (Dead Man's Switch, 可选)
# 配置后每次扫描成功会 GET 该 URL, 用于 healthchecks.io 或任意 HTTP ping 端点
# 当整个服务器/网络宕机时, 外部服务会因收不到 ping 而告警
HEARTBEAT_URL=https://hc-ping.com/your-uuid-here

# ============================================================
#  应急自动对冲 (P0-12) — 极度保守, 请仔细阅读!
# ============================================================
#
# ⚠️⚠️⚠️  安全警告  ⚠️⚠️⚠️
# 此功能会在特定条件下 *自动下单* 买入保护性 Long Put。
# 仅在你完全理解以下行为后才启用:
#   - 仅在强平距离 < 15% 且 TG 告警超时无人确认时触发
#   - 仅买入 Long Put (永不卖出/永不平仓)
#   - 每 24 小时最多 1 次
#   - 成本不超过 EMERGENCY_MAX_HEDGE_COST_USDT
#
# 默认关闭。设为 true 即视为你已接受自动交易风险。
EMERGENCY_AUTO_HEDGE=false
# 单次自动对冲最大花费 (USDT), 硬上限
EMERGENCY_MAX_HEDGE_COST_USDT=1000
# 发送 CRITICAL 告警后等待人工确认的时间 (分钟), 超时才自动执行
EMERGENCY_ACK_TIMEOUT_MIN=15
